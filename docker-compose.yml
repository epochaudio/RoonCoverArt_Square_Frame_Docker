services:
  coverart:
    container_name: coverart-app
    image: epochaudio/coverart:latest
    build: .
    network_mode: "host"
    restart: unless-stopped
    volumes:
      - ./images:/app/images:rw
      - ./config.json:/app/config.json:rw
